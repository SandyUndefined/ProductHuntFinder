# =================================
# Product Hunt Finder - Environment Variables
# =================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for production CORS)
FRONTEND_URL=https://your-frontend-domain.com

# =================================
# Authentication Configuration
# =================================

# Authentication method: "basic" or "token"
AUTH_METHOD=basic

# Basic Auth Credentials (used when AUTH_METHOD=basic)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Token Auth (used when AUTH_METHOD=token)
ADMIN_TOKEN=your-secure-token-here

# =================================
# LinkedIn Enrichment (Optional)
# =================================

# SerpAPI key for LinkedIn profile searches
# Get your key from: https://serpapi.com/
SERPAPI_API_KEY=your-serpapi-key-here

# =================================
# Caching & Scheduling Configuration
# =================================

# Cache expiry time in hours (default: 24)
CACHE_EXPIRY_HOURS=24

# Maximum in-memory cache size (default: 1000)
MAX_CACHE_SIZE=1000

# Cron job interval in hours (default: 4)
CRON_INTERVAL_HOURS=4

# =================================
# Google Sheets Integration (Optional)
# =================================

# Method 1: Individual environment variables (recommended for production)
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
GOOGLE_PROJECT_ID=your-google-project-id

# Method 2: Full JSON credentials as a single environment variable
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Google Sheets spreadsheet ID (required)
GOOGLE_SHEETS_ID=your-google-sheets-id

# Google Sheets tab/worksheet name (optional, defaults to "Approved Makers")
GOOGLE_SHEET_NAME=Approved Makers

# =================================
# Security & Performance
# =================================

# Set to true in production
SECURE_COOKIES=false

# Rate limiting (requests per minute)
RATE_LIMIT=100

# =================================
# Replit Configuration (if deploying on Replit)
# =================================

# Replit automatically sets these:
# - REPL_SLUG
# - REPL_OWNER
# - REPLIT_DB_URL (if using Replit Database)

# =================================
# Example Production Configuration
# =================================

# NODE_ENV=production
# PORT=443
# FRONTEND_URL=https://your-app.replit.app
# AUTH_METHOD=token
# ADMIN_TOKEN=super-secure-random-token-xyz789
# SERPAPI_API_KEY=your-production-serpapi-key
# CACHE_EXPIRY_HOURS=48
# CRON_INTERVAL_HOURS=6
# SECURE_COOKIES=true
